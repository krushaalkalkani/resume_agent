# Notion (optional — import only)
NOTION_TOKEN=

# Claude (tailoring + LaTeX auto-fix)
ANTHROPIC_API_KEY=

# Supabase (multi-user accounts + database)
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# Production: comma-separated frontend URLs allowed by CORS
# CORS_ORIGINS=https://your-app.vercel.app
